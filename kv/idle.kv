#:kivy 1.10.0
# idle.kv
# Idle screen (save pixels)

<ScreenIdle>:
    Button:
        canvas:
            Color:
                rgba: 0, 0, 0, 1
            Rectangle:
                pos: self.pos
                size: self.size
        on_press: root.manager.current='Language selection'